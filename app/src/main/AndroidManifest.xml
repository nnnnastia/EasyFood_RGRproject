<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.recipe_book_rgr">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recipe_Book_RGR"
        tools:targetApi="31">

        <!-- Головна активність -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Recipe_Book_RGR.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <activity android:name=".MainActivity"/>

        <!-- Додаткова активність -->
        <activity android:name=".recipe.AddEditRecipeActivity" />
        <activity android:name=".recipe.RecipeDetailsActivity"/>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".profile.LoginActivity" />
        <activity android:name=".profile.RegisterActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".profile.EditProfileActivity" />
        <activity android:name=".profile.MyRecipesActivity" />


    </application>

</manifest>
